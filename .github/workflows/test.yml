
name: test
on:
  push:
    branches: [ 'main' ]

jobs:
        
  setup-containers:
    strategy:
      matrix:
        os: [ 'el9' ]

    # TODO: Try [ 'linux', 'x64' ] instead.  Shouldn't matter for containers.
    runs-on: ubuntu-latest

    container:
      image: ghcr.io/perfsonar/unibuild/${{ matrix.os }}:latest

    steps:
      - run: echo Hello, world
